ColorSwatch is the circular color-picker dot used on every product page to switch pillowcase colorway + photo.

```jsx
<ColorSwatch color="Marfil" active onClick={() => setColor('Marfil')} />
<ColorSwatch color="negro" disabled />
```

Render a row of these for the 7-colorway picker; pair with the selected color name label below.
