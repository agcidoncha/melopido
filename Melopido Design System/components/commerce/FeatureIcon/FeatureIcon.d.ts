export interface FeatureIconProps {
  /** Path to a line-icon asset (e.g. from assets/icons/nav) */
  icon: string;
  /** Short benefit label, may include a `<br>` for two lines */
  label: string;
}
